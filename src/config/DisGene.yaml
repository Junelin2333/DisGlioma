TRAIN:
  train_batch_size: 32
  valid_batch_size: 16

  lr: 0.0002
  weight_decay: 0.00001
  min_lr: 0.00005

  min_epochs: 10
  max_epochs: 72
  patience: 10

  device: 1

MODEL:
  model_save_path: 'save_model'
  num_cluster: 3
  

DATA:
  fold: 1
  root_dir: 'data'
  ds_name: 'combined_data'
  csv_path: "data/clinical/combined_data.csv"

